@model IEnumerable<Prueba002.Models.dbModels.Marca>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marcas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Marcas)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMarcas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMarcas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMarcas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
